const OPTIONS = `Options:
  -p, --port  PORT Change port [default: 8545]
      --host  HOST Change host [default: localhost]
      --ssl   SSL Use ssl [default: http]
  
  --dry-run Perform dry run
`;
module.exports = {
  OPTIONS
}
